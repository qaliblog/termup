name: Build

on:
  push:
    branches:
      - master
      - 'github-releases/**'
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package_variant: [ apt-android-7 ]

    steps:
      - name: Clone repository
        uses: actions/checkout@v6

      - name: Setup java 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache TermUp assets
        uses: actions/cache@v5
        with:
          path: |
            build/termup-assets
            app/src/main/assets/termup-runtime
          key: termup-assets-${{ runner.os }}-${{ hashFiles('termup-assets/manifest.json') }}
          restore-keys: |
            termup-assets-${{ runner.os }}-

      - name: Setup QEMU for ARM64 emulation
        shell: bash {0}
        run: |
          echo "========================================="
          echo "  QEMU ARM64 Setup"
          echo "========================================="
          docker run --privileged --rm --pull=always tonistiigi/binfmt --install arm64
          echo "QEMU ARM64 emulation registered."
          echo ""

      - name: Build VNC native library
        shell: bash {0}
        run: |
          set -euo pipefail
          echo "========================================="
          echo "  Building VNC Native Library"
          echo "========================================="
          echo ""
          
          # Clone libvncserver if not present (CMake expects it at app/src/main/libvncserver)
          if [ ! -d "app/src/main/libvncserver" ]; then
            git clone --depth 1 --branch LibVNCServer-0.9.15 https://github.com/LibVNC/libvncserver.git app/src/main/libvncserver
          fi
          
          # Build libvncclient using cmake
          cd "${GITHUB_WORKSPACE}/app/src/main/libvncserver"
          mkdir -p build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release -DWITH_LIBSSL=OFF -DWITH_LIBSSH2=OFF -DWITH_LIBJPEG=OFF -DWITH_LIBPNG=OFF -DWITH_LZO=OFF -DWITH_ZLIB=ON -DBUILD_SHARED_LIBS=OFF ..
          make -j$(nproc) vncclient
          
          # Copy headers to a local include directory for the native-vnc CMake build
          mkdir -p "${GITHUB_WORKSPACE}/app/src/main/cpp/libvncserver_include"
          cp -r ../include/rfb "${GITHUB_WORKSPACE}/app/src/main/cpp/libvncserver_include/"
          
          # Copy built library to jniLibs using absolute paths (CMake expects app/src/jniLibs/)
          mkdir -p "${GITHUB_WORKSPACE}/app/src/jniLibs/arm64-v8a"
          cp "${GITHUB_WORKSPACE}/app/src/main/libvncserver/build/libvncclient.a" "${GITHUB_WORKSPACE}/app/src/jniLibs/arm64-v8a/libvncclient.a"
          
          echo "VNC native library built successfully"

      - name: Prepare TermUp assets
        shell: bash {0}
        run: |
          echo "========================================="
          echo "  TermUp Asset Preparation"
          echo "========================================="
          echo ""
          echo "Assets:"
          echo "  - proot-distro: downloading from GitHub"
          echo "  - Ubuntu 24.04: downloading official ubuntu-base rootfs"
          echo "  - Ubuntu 24.04: PRE-PROVISIONING (systemd, dbus, ca-certificates, locales)"
          echo "  - Box64: downloading prebuilt glibc ARM64 binary"
          echo "  - XFCE Desktop: downloading prebuilt XFCE desktop environment"
          echo ""

          # Show Docker version for debugging
          docker --version
          echo ""

          # Debug: check what prebuilt files exist
          ls -la "${GITHUB_WORKSPACE}/termbox-assets/prebuilt/box64/" || echo "prebuilt dir not found"

          # Run asset preparation (Box64 is REQUIRED - build fails if it cannot be produced)
          # provisionRootfs=true is now the default; explicitly pass it for clarity
          ./gradlew prepareTermUpAssets -PprovisionRootfs=true --info

      - name: Verify TermUp assets
        shell: bash {0}
        run: |
          echo "========================================="
          echo "  TermUp Asset Verification"
          echo "========================================="
          echo ""

          ASSETS_DIR="app/src/main/assets/termup-runtime"
          ERRORS=0

          # Check Bootstrap (handled by :app downloadBootstraps during assembleRelease)
          if [ -f "$ASSETS_DIR/bootstrap/aarch64.zip" ]; then
            echo "  Bootstrap:    PASS ($(du -h $ASSETS_DIR/bootstrap/aarch64.zip | cut -f1))"
          else
            echo "  Bootstrap:    SKIP (built by :app during assembleRelease)"
          fi

          # Check proot-distro
          if [ -d "$ASSETS_DIR/proot-distro" ]; then
            FILECOUNT=$(find $ASSETS_DIR/proot-distro -type f | wc -l)
            echo "  proot-distro: PASS ($FILECOUNT files)"
          else
            echo "  proot-distro: FAIL (missing)"
            ERRORS=$((ERRORS + 1))
          fi

          # Check Ubuntu rootfs (accepts .tar.gz or .tar.xz)
          UBUNTU_FILE=$(ls $ASSETS_DIR/ubuntu/ubuntu-24.04-arm64.tar.* 2>/dev/null | head -1)
          if [ -n "$UBUNTU_FILE" ]; then
            UBUNTU_SIZE=$(du -h "$UBUNTU_FILE" | cut -f1)
            echo "  Ubuntu ARM64: PASS ($UBUNTU_SIZE)"
          else
            echo "  Ubuntu ARM64: FAIL (missing)"
            ERRORS=$((ERRORS + 1))
          fi

          # Check proot (optional - download may fail in CI)
          if [ -f "$ASSETS_DIR/proot/bin/proot" ] && [ -f "$ASSETS_DIR/proot/lib/libandroid-shmem.so" ] && [ -f "$ASSETS_DIR/proot/lib/libtalloc.so.2" ]; then
            echo "  proot:         PASS ($(file $ASSETS_DIR/proot/bin/proot | grep -o 'ELF 64-bit.*' | head -1))"
          else
            echo "  proot:         WARN (missing binary or runtime libs - optional)"
          fi

          # Check Box64
          if [ -f "$ASSETS_DIR/box64/box64" ]; then
            BOX64_SIZE=$(du -h $ASSETS_DIR/box64/box64 | cut -f1)
            BOX64_TYPE=$(file $ASSETS_DIR/box64/box64)
            BOX64_SHA256=$(sha256sum $ASSETS_DIR/box64/box64 | cut -d' ' -f1)
            echo "  Box64 ARM64:  PASS ($BOX64_SIZE)"
            echo "               Type: $BOX64_TYPE"
            echo "               SHA256: $BOX64_SHA256"

            # Verify it's actually ARM64, not x86_64
            if echo "$BOX64_TYPE" | grep -qi "aarch64\|ARM aarch64"; then
              echo "               Architecture: VERIFIED (AArch64)"
            else
              echo "  Box64 ARM64:  FAIL (wrong architecture!)"
              ERRORS=$((ERRORS + 1))
            fi
          else
            echo "  Box64 ARM64:  FAIL (missing)"
            ERRORS=$((ERRORS + 1))
          fi

          # Check Box64 ELF details
          if [ -f "$ASSETS_DIR/box64/box64" ] && command -v readelf &> /dev/null; then
            echo ""
            echo "  Box64 ELF details:"
            readelf -h $ASSETS_DIR/box64/box64 2>/dev/null | grep -E "Machine:|Class:|Type:|Entry point" || echo "  (readelf check failed)"
          fi

          # Check Box64 transparent layer (on-disk x86_64 exec wrapping).
          # termbox-x86_64-launcher replaces every x86_64 ELF on disk, termbox-scan64
          # scans for x86_64 executables, termbox-wrapd keeps the wrap table fresh so
          # binaries spawned by subprocesses (e.g. aapt2 under gradlew) get wrapped too.
          # These replaced the legacy libbox64auto.so LD_PRELOAD interposer.
          for LAYER_BIN in termup-x86_64-launcher termup-scan64 termup-wrapd; do
            LAYER_PATH="$ASSETS_DIR/box64/$LAYER_BIN"
            if [ -f "$LAYER_PATH" ]; then
              LAYER_TYPE=$(file "$LAYER_PATH")
              echo "  $LAYER_BIN: PASS ($(du -h "$LAYER_PATH" | cut -f1))"
              echo "               Type: $LAYER_TYPE"
              if echo "$LAYER_TYPE" | grep -qi "aarch64\\|ARM aarch64"; then
                echo "               Architecture: VERIFIED (AArch64)"
              else
                echo "  $LAYER_BIN:   FAIL (wrong architecture!)"
                ERRORS=$((ERRORS + 1))
              fi
            else
              echo "  $LAYER_BIN:   FAIL (missing)"
              ERRORS=$((ERRORS + 1))
            fi
          done

          # Check Box64 x86_64 libraries (glibc runtime for emulated binaries) - optional
          if [ -f "$ASSETS_DIR/box64-libs/box64-libs.tar.gz" ]; then
            LIBS_SIZE=$(du -h $ASSETS_DIR/box64-libs/box64-libs.tar.gz | cut -f1)
            echo "  box64-libs:   PASS ($LIBS_SIZE)"
            if command -v tar &> /dev/null; then
              HAS_LD=$(tar tzf $ASSETS_DIR/box64-libs/box64-libs.tar.gz 2>/dev/null | grep -c "lib64/ld-linux-x86-64.so.2" || true)
              HAS_LIBC=$(tar tzf $ASSETS_DIR/box64-libs/box64-libs.tar.gz 2>/dev/null | grep -c "lib/x86_64-linux-gnu/libc.so.6" || true)
              if [ "$HAS_LD" -gt 0 ] && [ "$HAS_LIBC" -gt 0 ]; then
                echo "               Contents: VERIFIED (ld-linux + libc present)"
              else
                echo "  box64-libs:   WARN (missing ld-linux-x86-64.so.2 or libc.so.6 in archive - optional)"
              fi
            fi
          else
            echo "  box64-libs:   WARN (missing - optional)"
          fi

          # Check XFCE Desktop (optional)
          if [ -d "$ASSETS_DIR/xfce/overlay" ]; then
            XFCE_FILES=$(find $ASSETS_DIR/xfce/overlay -type f | wc -l)
            echo "  XFCE Desktop: PASS ($XFCE_FILES files)"
          else
            echo "  XFCE Desktop: WARN (missing - optional)"
          fi

          echo ""
          echo "========================================="
          if [ $ERRORS -gt 0 ]; then
            echo "  RESULT: FAILED ($ERRORS error(s))"
            echo "========================================="
            exit 1
          else
            echo "  RESULT: ALL ASSETS VERIFIED"
            echo "========================================="
          fi

      - name: Build release APKs
        shell: bash {0}
        env:
          PACKAGE_VARIANT: ${{ matrix.package_variant }}
        run: |
          exit_on_error() { echo "$1"; exit 1; }

          echo "Setting vars"

          if [ "$GITHUB_EVENT_NAME" == "pull_request" ]; then
              GITHUB_SHA="${{ github.event.pull_request.head.sha }}"
          fi

          # Set RELEASE_VERSION_NAME
          CURRENT_VERSION_NAME_REGEX='\s+versionName "([^"]+)"$'
          CURRENT_VERSION_NAME="$(grep -m 1 -E "$CURRENT_VERSION_NAME_REGEX" ./app/build.gradle | sed -r "s/$CURRENT_VERSION_NAME_REGEX/\1/")"
          RELEASE_VERSION_NAME="v$CURRENT_VERSION_NAME+${GITHUB_SHA:0:7}"
          if ! printf "%s" "${RELEASE_VERSION_NAME/v/}" | grep -qP '^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$'; then
           exit_on_error "The versionName '${RELEASE_VERSION_NAME/v/}' is not a valid semver."
          fi

          APK_DIR_PATH="./app/build/outputs/apk/release"
          APK_VERSION_TAG="$RELEASE_VERSION_NAME-${{ env.PACKAGE_VARIANT }}-github-release"
          APK_BASENAME_PREFIX="termup_$APK_VERSION_TAG"

          echo "APK_DIR_PATH=$APK_DIR_PATH" >> $GITHUB_ENV
          echo "APK_VERSION_TAG=$APK_VERSION_TAG" >> $GITHUB_ENV
          echo "APK_BASENAME_PREFIX=$APK_BASENAME_PREFIX" >> $GITHUB_ENV

          echo "Building release APKs for '$APK_VERSION_TAG'"
          export TERMUX_APP_VERSION_NAME="${RELEASE_VERSION_NAME/v/}"
          export TERMUX_APK_VERSION_TAG="$APK_VERSION_TAG"
          export TERMUX_PACKAGE_VARIANT="${{ env.PACKAGE_VARIANT }}"
          export TERMUX_SPLIT_APKS_FOR_RELEASE_BUILDS="1"

          if ! ./gradlew assembleRelease; then
            exit_on_error "Release build failed."
          fi

          echo "Built APKs:"
          ls -lh "$APK_DIR_PATH"/*.apk 2>/dev/null || exit_on_error "No APKs found in $APK_DIR_PATH"

          echo "Generating sha256sums"
          (cd "$APK_DIR_PATH" && sha256sum *.apk > "${APK_BASENAME_PREFIX}_sha256sums") || true

      - name: Upload APKs
        uses: actions/upload-artifact@v6
        with:
          name: termup-release-${{ env.PACKAGE_VARIANT }}
          path: |
            ${{ env.APK_DIR_PATH }}/*.apk
            ${{ env.APK_DIR_PATH }}/output-metadata.json
          if-no-files-found: error
